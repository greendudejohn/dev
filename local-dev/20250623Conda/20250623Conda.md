conda create --name 20250623Conda python=3.10.0 -y
conda activate 20250623Conda
conda install -c conda-forge tensorflow jupyterlab ffmpeg -y
conda install -c conda-forge pandas
# Here we are creating 20250623Conda
# We observes which package are installed 20250623Conda
pip install -r requirements.txt
pip list
conda deactivate
pip list
conda activate help
whereis python
whereis phyton3 

pyton app.py

jupyter lab --no-browser --allow-root --ip 0.0.0.0


import pandas as pd

# Step 1: Create a DataFrame
data = {
    'Name': ['Alice', 'Bob', 'Charlie', 'David'],
    'Age': [25, 30, 35, 40],
    'Salary': [50000, 60000, 70000, 80000]
}
df = pd.DataFrame(data)

# Step 2: Show the DataFrame
print("Original DataFrame:")
print(df)

# Step 3: Calculate average salary
average_salary = df['Salary'].mean()
print("\nAverage Salary:", average_salary)

# Step 4: Filter people older than 30
older_than_30 = df[df['Age'] > 30]
print("\nPeople older than 30:")
print(older_than_30)
